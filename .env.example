GROQ_API_KEY=gsk_your_groq_api_key_here
EXPO_PUBLIC_OPENAI_API_KEY=sk_your_openai_api_key_here
# Local development URL for your proxy server that hides the API key from mobile clients.
# Use the actual IP address of your development machine if serving to a real device.
PROXY_URL=http://localhost:3000/groq
# For Vercel: PROXY_URL=https://<your-vercel-app>.vercel.app/api/groq

## NOTES
# 1) Do NOT commit .env with keys into version control. Add a .env file based on this example.
# 2) On Android emulator use http://10.0.2.2:3000/groq as PROXY_URL to reach the host machine.